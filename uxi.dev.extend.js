/* eslint-disable no-param-reassign */
module.exports = (config) => {
  'r';


  // config.devServer.port = 3100;
  // config.devServer.port = 3100;
  // config.entry[1] = 'webpack-dev-server/client?http://localhost:3100';

  return config;
};
